# Trout
=================

Trout is the reconstruction package for the SHiP software framework. It runs on either the output of the Shannon digitisation, or the real detector data.

Trout depends on `data-model`, `geometry` and the field service.